# TODO

## Obsidian compatibility and UI

- [x] Verify the true minimum supported Obsidian version and update `minAppVersion` / `versions.json` if needed.
- [ ] Decide whether to rename the command display text from `Validate current note` to `Validate active note` to match Obsidian terminology, while keeping the command ID stable.
